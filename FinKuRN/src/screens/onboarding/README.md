# Onboarding Screens

온보딩 화면 모듈입니다. 신규 사용자가 앱을 처음 사용할 때 필요한 정보를 수집하고 설정하는 5단계 프로세스를 담당합니다.

## 📁 파일 구조

```
src/screens/onboarding/
├── README.md                        # 이 파일
├── index.ts                         # Export 통합 파일
├── OnboardingWelcomeScreen.tsx      # 1단계: 환영 화면
├── OnboardingGoalsScreen.tsx        # 2단계: 목표 선택
├── OnboardingBasicInfoScreen.tsx    # 3단계: 기본 정보 입력
├── OnboardingConsentScreen.tsx      # 4단계: 알림 및 동의
└── OnboardingCompleteScreen.tsx     # 5단계: 완료
```

## 🔄 온보딩 플로우

```
Login/Signup
    ↓
[1] OnboardingWelcomeScreen
    ↓ [시작하기]
[2] OnboardingGoalsScreen ← [뒤로가기]
    ↓ [다음]
[3] OnboardingBasicInfoScreen ← [뒤로가기]
    ↓ [다음]
[4] OnboardingConsentScreen ← [뒤로가기]
    ↓ [완료]
[5] OnboardingCompleteScreen
    ↓ [핀쿠와 함께 시작하기]
Main Screen (홈 탭)
```

---

## 📄 파일별 상세 설명

### 1. `index.ts`
**역할**: 모든 온보딩 화면을 중앙에서 export하여 import를 간소화합니다.

**사용 예시**:
```typescript
import {
  OnboardingWelcomeScreen,
  OnboardingGoalsScreen,
  OnboardingBasicInfoScreen,
  OnboardingConsentScreen,
  OnboardingCompleteScreen
} from '@/screens/onboarding';
```

---

### 2. `OnboardingWelcomeScreen.tsx`
**파일 위치**: `src/screens/onboarding/OnboardingWelcomeScreen.tsx`

**목적**: 온보딩의 첫 번째 화면으로, 핀쿠를 소개하고 서비스의 핵심 기능을 안내합니다.

**주요 기능**:
- 펭귄 캐릭터 이미지 표시
- 앱 이름 및 슬로건 표시
- 3가지 핵심 기능 소개
  - 맞춤형 금융 정보
  - 실천 가능한 체크리스트
  - 목표 달성 리워드
- 진행 상태 표시 (1/4)

**UI 구성**:
```
┌─────────────────────────┐
│     [펭귄 이미지]        │
│                          │
│   안녕하세요!            │
│   핀쿠입니다             │
│                          │
│   Financial Knowledge    │
│   & Resource Navigator   │
│                          │
│ ┌────────────────────┐  │
│ │ • 맞춤형 금융 정보  │  │
│ │ • 실천 가능한 체크  │  │
│ │ • 목표 달성 리워드  │  │
│ └────────────────────┘  │
│                          │
│   [ 시작하기 ]          │
│                          │
│     ● ○ ○ ○             │
└─────────────────────────┘
```

**핵심 코드**:
```typescript
const handleStart = () => {
  navigation.navigate('OnboardingGoals' as any);
};
```

**스타일 특징**:
- 배경: `HOME_GRADIENTS` 그라디언트
- 카드: `borderRadius.xxxl` (32px), 흰색 배경
- Bullet 포인트: 6px 파란색 원형

---

### 3. `OnboardingGoalsScreen.tsx`
**파일 위치**: `src/screens/onboarding/OnboardingGoalsScreen.tsx`

**목적**: 사용자의 금융 목표를 선택받습니다 (최대 3개).

**주요 기능**:
- 4가지 목표 옵션 제공
  - 저축: 목돈 모으기와 저축 상품 찾기
  - 학자금: 학자금 대출과 상환 관리
  - 신용관리: 신용점수 향상과 카드 관리
  - 투자기초: 첫 투자 시작과 기본 지식
- 다중 선택 가능 (최대 3개)
- 선택 개수 실시간 표시
- 뒤로가기 버튼
- 진행 상태 표시 (2/4)

**UI 구성**:
```
┌─────────────────────────┐
│ ←                        │
│                          │
│ 어떤 목표를 가지고       │
│ 계신가요?                │
│                          │
│ 최대 3개까지 선택        │
│                          │
│ ┌────────────────────┐  │
│ │ 저축          ✓    │  │ (선택됨)
│ │ 목돈 모으기...     │  │
│ └────────────────────┘  │
│                          │
│ ┌────────────────────┐  │
│ │ 학자금             │  │
│ │ 학자금 대출...     │  │
│ └────────────────────┘  │
│                          │
│ 2/3 선택됨               │
│                          │
│   [ 다음 ]              │
│                          │
│     ○ ● ○ ○             │
└─────────────────────────┘
```

**상태 관리**:
```typescript
const [selectedGoals, setSelectedGoals] = useState<UserGoal[]>([]);

const handleToggleGoal = (goal: UserGoal) => {
  if (selectedGoals.includes(goal)) {
    setSelectedGoals(selectedGoals.filter((g) => g !== goal));
  } else {
    if (selectedGoals.length >= 3) return; // 최대 3개 제한
    setSelectedGoals([...selectedGoals, goal]);
  }
};
```

**데이터 타입**:
```typescript
type UserGoal = '저축' | '학자금' | '신용관리' | '투자기초';
```

---

### 4. `OnboardingBasicInfoScreen.tsx`
**파일 위치**: `src/screens/onboarding/OnboardingBasicInfoScreen.tsx`

**목적**: 맞춤형 금융 정보 제공을 위한 기본 정보를 수집합니다.

**주요 기능**:
- **연령 입력**: 숫자만 입력 가능 (만 나이)
- **직업 선택**: 6가지 카테고리
  - 학생
  - 취업준비생
  - 직장인
  - 프리랜서
  - 자영업
  - 기타
- **월 소득 선택**: 5가지 구간
  - 100만원 미만
  - 100-200만원
  - 200-300만원
  - 300-400만원
  - 400만원 이상
- **거주 지역 입력**: 텍스트 입력 (예: "서울특별시 강남구")
- 모든 필드 입력 시 다음 버튼 활성화
- 뒤로가기 버튼
- 진행 상태 표시 (3/4)

**UI 구성**:
```
┌─────────────────────────┐
│ ←                        │
│                          │
│ 기본 정보를              │
│ 입력해주세요             │
│                          │
│ 맞춤형 금융 정보 제공을  │
│ 위해 필요해요            │
│                          │
│ 연령                     │
│ [_______________]        │
│                          │
│ 직업                     │
│ [학생] [취업] [직장인]   │
│ [프리] [자영] [기타]     │
│                          │
│ 월 소득                  │
│ [ 100만원 미만 ]         │
│ [ 100-200만원 ]          │
│ ...                      │
│                          │
│ 거주 지역                │
│ [_______________]        │
│                          │
│   [ 다음 ]              │
│                          │
│     ○ ○ ● ○             │
└─────────────────────────┘
```

**입력 검증**:
```typescript
// 연령: 숫자만 허용
const handleAgeChange = (value: string) => {
  const numericValue = value.replace(/[^0-9]/g, '');
  setAge(numericValue);
};

// 다음 버튼 활성화 조건
const isNextButtonEnabled =
  age.length > 0 &&
  jobCategory !== null &&
  incomeRange !== null &&
  region.trim().length > 0;
```

**데이터 타입**:
```typescript
type JobCategory = '학생' | '취업준비생' | '직장인' | '프리랜서' | '자영업' | '기타';
type IncomeRange = '100만원 미만' | '100-200만원' | '200-300만원' | '300-400만원' | '400만원 이상';

interface BasicInfoData {
  age: number;
  jobCategory: JobCategory;
  incomeRange: IncomeRange;
  region: string;
}
```

---

### 5. `OnboardingConsentScreen.tsx`
**파일 위치**: `src/screens/onboarding/OnboardingConsentScreen.tsx`

**목적**: 알림 및 리워드 프로그램 참여 동의를 받습니다.

**주요 기능**:
- **전체 동의** 버튼으로 일괄 동의
- 개별 동의 항목 (모두 선택사항)
  - 푸시 알림: 마감일 알림과 맞춤형 금융 정보
  - 마케팅 알림: 새로운 혜택과 이벤트 소식
  - 리워드 프로그램: 목표 달성 시 핀쿠 포인트
- 안내 문구 표시
- 뒤로가기 버튼
- 진행 상태 표시 (4/4)

**UI 구성**:
```
┌─────────────────────────┐
│ ←                        │
│                          │
│ 알림 설정을              │
│ 완료해주세요             │
│                          │
│ 언제든지 설정에서        │
│ 변경할 수 있어요         │
│                          │
│ ┌────────────────────┐  │
│ │ ✓ 전체 동의        │  │
│ └────────────────────┘  │
│                          │
│ 🔔 푸시 알림         ✓  │
│ 마감일 알림과...         │
│                          │
│ 📬 마케팅 알림       ✓  │
│ 새로운 혜택과...         │
│                          │
│ 🎁 리워드 프로그램   ○  │
│ 목표 달성 시...          │
│                          │
│ 💡 알림을 허용하면       │
│ 마감일을 놓치지 않고...  │
│                          │
│   [ 완료 ]              │
│                          │
│     ○ ○ ○ ●             │
└─────────────────────────┘
```

**상태 관리**:
```typescript
const [consents, setConsents] = useState({
  pushNotification: false,
  marketingNotification: false,
  rewardProgram: false,
});

// 전체 동의
const handleAgreeAll = () => {
  setConsents({
    pushNotification: true,
    marketingNotification: true,
    rewardProgram: true,
  });
};

// 개별 토글
const handleToggleConsent = (key: string) => {
  setConsents((prev) => ({
    ...prev,
    [key]: !prev[key as keyof typeof prev],
  }));
};
```

**데이터 타입**:
```typescript
interface ConsentData {
  pushNotification: boolean;
  marketingNotification: boolean;
  rewardProgram: boolean;
}
```

---

### 6. `OnboardingCompleteScreen.tsx`
**파일 위치**: `src/screens/onboarding/OnboardingCompleteScreen.tsx`

**목적**: 온보딩 완료를 축하하고 메인 앱으로 진입합니다.

**주요 기능**:
- 완료 축하 메시지
- 펭귄 캐릭터 표시
- 핵심 기능 재안내
- "핀쿠와 함께 시작하기" 버튼으로 메인 화면 진입
- 네비게이션 스택 리셋 (뒤로가기 방지)

**UI 구성**:
```
┌─────────────────────────┐
│                          │
│     [펭귄 이미지]        │
│                          │
│ 모든 준비가              │
│ 완료되었어요!            │
│                          │
│ 이제 핀쿠와 함께         │
│ 똑똑한 금융 생활을       │
│ 시작해보세요             │
│                          │
│ ┌────────────────────┐  │
│ │ • 맞춤형 금융 정보  │  │
│ │   선택한 목표에...  │  │
│ │                     │  │
│ │ • 실천 가능한...    │  │
│ │   복잡한 절차를...  │  │
│ │                     │  │
│ │ • 목표 달성 리워드  │  │
│ │   핀쿠 포인트를...  │  │
│ └────────────────────┘  │
│                          │
│ [ 핀쿠와 함께 시작하기 ]│
└─────────────────────────┘
```

**네비게이션 처리**:
```typescript
const handleStart = async () => {
  try {
    // TODO: AsyncStorage에 온보딩 완료 저장
    // await AsyncStorage.setItem('onboarding_completed', 'true');

    // TODO: 백엔드 API 호출
    // await submitOnboardingData(onboardingData);

    // 메인 화면으로 이동 (네비게이션 스택 리셋)
    navigation.reset({
      index: 0,
      routes: [{ name: 'Main' as any }],
    });
  } catch (error) {
    console.error('Failed to complete onboarding:', error);
  }
};
```

**중요 사항**:
- `navigation.reset()` 사용으로 사용자가 뒤로가기를 눌러도 온보딩으로 돌아갈 수 없음
- 온보딩 완료 상태를 AsyncStorage에 저장해야 함 (추후 구현 필요)

---

## 🎨 공통 디자인 시스템

### 색상
- **Primary**: `theme.colors.primary` - 파란색 (버튼, 선택 표시)
- **Background**: `theme.colors.background` - 앱 배경색
- **White**: `theme.colors.white` - 카드 배경
- **Text Primary**: `theme.colors.textPrimary` - 주요 텍스트
- **Text Secondary**: `theme.colors.textSecondary` - 보조 텍스트

### 타이포그래피
- **제목**: `theme.typography.heading1` (28-32px, bold)
- **부제목**: 15-16px, regular
- **본문**: 13-15px, medium/regular
- **Letter Spacing**: -0.2 ~ -0.3

### 레이아웃
- **카드**: `borderRadius.xxxl` (32px)
- **버튼**: `borderRadius.full` (완전 둥근 모양)
- **뒤로가기 버튼**:
  - 위치: `top: theme.spacing.xxxl + 10, left: theme.spacing.lg`
  - 크기: 40x40px
  - 배경: `rgba(255, 255, 255, 0.9)` + 그림자

### Progress Dots
- **비활성**: 8x8px, `rgba(0, 0, 0, 0.1)`
- **활성**: 24x8px, `theme.colors.primary`

---

## 🔗 관련 파일

### 타입 정의
- `src/types/onboarding.ts`: 온보딩 관련 모든 타입 정의

### API
- `src/api/onboardingApi.ts`: 온보딩 데이터 제출 및 조회 API

### 네비게이션
- `src/navigation/MainNavigator.tsx`: 온보딩 화면 라우트 등록

---

## 📝 TODO

### 즉시 구현 필요
- [ ] AsyncStorage에 온보딩 완료 플래그 저장
- [ ] 앱 시작 시 온보딩 완료 여부 확인
- [ ] 실제 API 연동 (Mock → Real API)

### 추후 개선
- [ ] Context API로 온보딩 데이터 전역 관리
- [ ] 진행 상황 임시 저장 (중간 이탈 시)
- [ ] 에러 처리 및 Toast 메시지
- [ ] 로딩 인디케이터 추가
- [ ] 완료 화면 애니메이션 효과

---

## 🚀 사용 예시

### 네비게이션에서 온보딩 시작
```typescript
// LoginScreen.tsx
navigation.navigate('OnboardingWelcome');
```

### 온보딩 데이터 수집 (추후 Context 사용 권장)
```typescript
const onboardingData: OnboardingData = {
  goals: { selectedGoals: ['저축', '투자기초'] },
  basicInfo: {
    age: 25,
    jobCategory: '직장인',
    incomeRange: '200-300만원',
    region: '서울특별시 강남구'
  },
  consent: {
    pushNotification: true,
    marketingNotification: false,
    rewardProgram: true
  }
};
```

---

## 📚 참고 문서
- [ONBOARDING_README.md](../../ONBOARDING_README.md) - 전체 온보딩 시스템 가이드
- [타입 정의](../../types/onboarding.ts)
- [API 함수](../../api/onboardingApi.ts)
